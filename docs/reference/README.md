# Reference files

These files were preserved from the previous site package for historical reference. They are not connected to the active contact or order forms.

The production forms in `index.html` and `order.html` submit to `/api/contact` and `/api/order`. Do not copy an old form action back into those pages, and do not add a Formspree endpoint.
